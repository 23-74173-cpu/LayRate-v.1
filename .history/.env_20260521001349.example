# No required environment variables are currently used by the Vite app.
#
# Vite only exposes client-side environment variables prefixed with VITE_.
# Add future public client config here with placeholder values, for example:
# VITE_API_BASE_URL=https://api.example.com
